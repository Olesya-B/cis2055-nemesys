@model CommentViewModel

@{ 
    <div>
        <form method="post" asp-controller="BlogPost" asp-action="Comment">
            <input asp-for='PostId' type="hidden" />
            <input asp-for='Message' />
            <button type="submit"> Submit Comment </button>
        </form>
    </div>
}